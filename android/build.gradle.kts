// Personal AI Agent — Android Device Agent V0
// Project-level build file. Deliberately minimal: only the Android and
// Kotlin plugins needed to build a single app module.
plugins {
    id("com.android.application") version "8.7.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
